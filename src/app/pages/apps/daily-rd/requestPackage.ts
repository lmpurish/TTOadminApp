import { Route } from "@angular/router";

export interface requestPackage {
  id: number;
  packageId :number;
  imageName : string;
  imageUrl : string;
  uploadedBy : string;
  description: string;
 
}
